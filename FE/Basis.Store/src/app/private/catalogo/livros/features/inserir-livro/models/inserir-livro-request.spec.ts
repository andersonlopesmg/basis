import { InserirLivroRequest } from './inserir-livro-request';

describe('InserirLivroRequest', () => {
  it('should create an instance', () => {
    expect(new InserirLivroRequest()).toBeTruthy();
  });
});
